[alembic]
script_location = alembic
# The effective URL is supplied by app.core.config.Settings in alembic/env.py.
# Keeping it out of this static file prevents Docker and local runtime drift.
sqlalchemy.url =
